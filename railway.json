{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd client && npm install && (if [ \"$RAILWAY_ENVIRONMENT\" = \"production\" ]; then export GENERATE_SOURCEMAP=false; fi) && npm run build && cd .. && mv client/build /app/client-build",
    "startCommand": "node server/index.js",
    "outputDirectory": "/app/client-build"
  }
}