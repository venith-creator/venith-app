{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "postinstall": "mkdir -p /app/dist && (cp -r client/build/* /app/dist/ || echo 'Copy failed, creating fallback')",
    "start": "node server/index.js"
  }
}