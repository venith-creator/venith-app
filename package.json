{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "postinstall": " mkdir -p /app/dist &&  cp -r client/build/* /app/dist/ ||  (echo 'FALLBACK: Creating empty dist' && mkdir -p /app/dist && echo '<h1>MANUAL FALLBACK</h1>' > /app/dist/index.html)",
    "start": " echo 'DIST CONTENTS:' &&   (ls -la /app/dist || echo 'NO DIST FOLDER') &&  node server/index.js"
  }
}